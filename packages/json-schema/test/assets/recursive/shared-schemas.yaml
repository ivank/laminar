components:
  schemas:
    HttpError:
      additionalProperties: false
      properties:
        message:
          $ref: './schema.yaml#/components/schemas/Message'
      required:
        - message
