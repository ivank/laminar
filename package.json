{
  "name": "ts-rest",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "jest": {
    "preset": "./jest.json"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
